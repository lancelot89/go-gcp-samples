# Environment variables for v2-firestore

# Port number for the server (default: 8080)
PORT=8080

# Google Cloud Project ID (required)
PROJECT_ID=your-project-id

# Firestore Emulator Host (optional, for local development)
# FIRESTORE_EMULATOR_HOST=localhost:8081

# Google Cloud Region (required for deployment)
# REGION=asia-northeast1

# Cloud Run Service Name
# SERVICE_NAME=go-firestore-app

# Application Default Credentials (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json